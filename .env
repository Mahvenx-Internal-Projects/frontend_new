# Default API URL used when no mode-specific env file is present
# Development overrides this with .env.development
# Production overrides this with .env.production
VITE_API_URL=https://lms.worksupport360.com